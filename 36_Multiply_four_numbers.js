/*

Take four parameters. Multiply the four numbers and then return the result

*/

function multi(a, b, c, d) {
    const result = a * b * c * d;
    return result;
}

const res = multi(2, 5, 2, 3);
console.log(res);